#include <stdio.h>
#include <stdlib.h>

int main()
{
    char * s = "CECI EST UNE CHAINE DE CARACTERES";
    
    int a = 3 + 256 * 10 + 4 * 256 * 256 + 11 * 256 * 256 * 256;
    
    system("ls -l");
    system("id");
    
    return 0;
}